Purpose: Pure helper functions.

Contents: unitConversion.ts for data speeds, validation.ts for API keys, and cn.ts for Tailwind class merging.

Rule: Functions here must be "pure" (no side effects, same input always equals same output).