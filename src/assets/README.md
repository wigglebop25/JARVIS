Purpose: Storage for static files and visual identity.

Contents: JARVIS branding, custom SVG icons for device states (Online/Offline), and UI sound effects for wake-word feedback.

Rule: Do not store large binaries here; use external hosting or the public/ folder for non-compiled assets.