Purpose: Centralized TypeScript definitions.

Contents: Interfaces for Device, AutomationRoutine, and the MCPCommand schema.

Rule: Align these types with your Rust structs in src-tauri to ensure type safety across the bridge.